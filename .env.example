# Server
PORT=3000
NODE_ENV=development

# Downstream service this BFF aggregates (REST, typed client from @demo/contracts)
PAYMENT_SERVICE_URL=http://localhost:3004

# Auth — Cognito
# Set AUTH_DISABLED=true locally when you don't have a Cognito pool
AUTH_DISABLED=false
# https://cognito-idp.{region}.amazonaws.com/{userPoolId}
COGNITO_ISSUER=
# App client ID (aud claim in the id_token)
COGNITO_AUDIENCE=

# Observability — OpenTelemetry
# Leave blank to use ConsoleSpanExporter (prints to stdout) for local dev
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_SERVICE_NAME=payment-bff
